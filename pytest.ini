[pytest]
asyncio_mode = auto
markers =
    integration: hits the live Gemini API (requires ADC, costs money)
    loadtest: sustained load benchmark against the live Gemini API (requires ADC, up to $1.50 per run)
addopts = -m "not integration and not loadtest"
